<%= form_for User.first do |f| %>
  Films:
  <%= f.file_field :films_file %>
  Admin:
  <%= f.file_field :admin_file %>
  Theaters:
  <%= f.file_field :theaters_file %>
  <input type="submit" />
<% end %>
